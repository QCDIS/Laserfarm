name: laserfarm

channels:
  - conda-forge
  - defaults

dependencies:
  - python>=3.7
  - pdal
  - python-pdal
  - gdal
  - shapely  # needed to install the correct version of geos
  - lazrs-python >= 0.5.0
  - pip
  - pip:
      - -r requirements.txt
      - .
